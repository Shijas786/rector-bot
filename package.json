{
    "name": "rector-monorepo",
    "version": "1.0.0",
    "description": "Root package to enforce Node.js environment on Railway",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "openclaw": "^0.0.1"
    },
    "scripts": {
        "postinstall": "cd agent && npm install && npx prisma generate && npm run build"
    }
}